{
  "name": "react-demo",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"esbuild ./src/index.tsx --tsconfig=tsconfig.json --watch --bundle --outdir=./dist/js\" \"npx live-server dist\"",
    "build": "esbuild ./src/index.tsx --outdir=./dist/js --bundle"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^22.9.0",
    "concurrently": "^9.1.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  }
}
